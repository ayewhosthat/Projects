A very basic binary text classification model using the overall rating that the reviewer gave the anime as the sole feature that is given to the model.

The Jupyter Notebook also includes a little bit of data analysis in regards to the data obtained

Some next steps could include using a neural network to see if that yields a better result than our basic model.
